ContainerStatistic:
  type: object
  config:
    resolveField: '@=resolver("App\\UserInterface\\GraphQL\\Resolver\\ContainerStatisticResolver", [info, value, args])'
    fields:
      containerName:
        type: String!
      stat:
        type: String!
