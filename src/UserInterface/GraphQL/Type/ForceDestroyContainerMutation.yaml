ForceDestroyContainerInput:
  type: input-object
  config:
    fields:
      name:
        type: String!

ForceDestroyContainerOutput:
  type: union
  config:
    types: [SuccessOutput, FailedOutput]
