monolog:
    handlers:
        main:
            type: fingers_crossed
            action_level: error
            handler: loki
            excluded_http_codes: [404, 405]
            channels: ["!event"]
